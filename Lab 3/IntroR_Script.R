# Sets sales to 350 (equivalent to sales = 350)
sales <- 350

# Sets royalty to 7 (equivalent to royalty = 7)
royalty <- 7

# Calculates revenue as sales times royalty
# Equivalent to royalty = sales=350 * royalty=7 so royalty = 350 * 7 = 2450
revenue <- sales * royalty

# Prints revenue to the console (2450)
revenue

# Get the square root of 225
sqrt(225)

mean(1:6)
median(1:6)

library(ggplot2)

